import 'package:crea_chess/router/.shared/route_body.dart';

/// A route selectable from the bottom navigation bar
abstract class BottomRouteBody extends RouteBody {
  const BottomRouteBody({super.key});
}
