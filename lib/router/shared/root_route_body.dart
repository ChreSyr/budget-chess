import 'package:crea_chess/router/shared/route_body.dart';

/// A route without parent.
///
/// In the app, it is a route selectable from the bottom navigation bar.
abstract class RootRouteBody extends RouteBody {
  const RootRouteBody({super.key});
}
