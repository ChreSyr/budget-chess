import 'package:crea_chess/package/atomic_design/size.dart';
import 'package:flutter/material.dart';

class CCBorderRadiusCircular {
  /// BorderRadius.circular(4)
  static const xsmall = BorderRadius.all(Radius.circular(CCSize.xsmall));

  /// BorderRadius.circular(8)
  static const small = BorderRadius.all(Radius.circular(CCSize.small));

  /// BorderRadius.circular(16)
  static const medium = BorderRadius.all(Radius.circular(CCSize.medium));
}
